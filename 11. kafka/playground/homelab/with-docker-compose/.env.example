# ./start.sh generates KAFKA_CLUSTER_ID automatically.
# All three Kafka nodes must use the same ID.
KAFKA_CLUSTER_ID=

# Local machine defaults.
KAFKA_BIND_ADDRESS=127.0.0.1
KAFKA_EXTERNAL_HOST=localhost

KAFKA_UI_BIND_ADDRESS=127.0.0.1
KAFKA_UI_PORT=8080
