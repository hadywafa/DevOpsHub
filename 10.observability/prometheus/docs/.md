# 🧩 Prometheus Authentication

✅ **1. Prometheus itself has no built-in user login.**  
If you open `http://localhost:9090`, there’s **no username or password**.
Prometheus trusts that it’s running in a **secure internal network**.

---

✅ **2. Authentication is usually for _exporters or targets_.**  
When Prometheus scrapes metrics, it may need to **authenticate TO** those targets.
For example:

```yaml
scrape_configs:
  - job_name: "secure_node"
    static_configs:
      - targets: ["node1:9100"]
    basic_auth:
      username: "prometheus"
      password: "s3cr3t"
```

So Prometheus uses auth credentials to **pull data from the exporter**.

---

✅ **3. If you want to protect Prometheus itself (the UI or API)**  
You must add **security externally**, such as:

- Put **Nginx** or **Traefik** in front of it with Basic Auth or OAuth2
- Enable **HTTPS** and **Basic Auth** in a `web.yml` file

Example:

```yaml
basic_auth_users:
  admin: "$2b$12$...bcrypt_hash..."
```

---

## 🔒 TL;DR

| Purpose                       | Where Auth Happens                         | Example                      |
| ----------------------------- | ------------------------------------------ | ---------------------------- |
| **Prometheus → Exporter**     | Prometheus authenticates _to_ targets      | `basic_auth`, `bearer_token` |
| **User → Prometheus UI**      | External proxy or `web.yml` handles login  | Nginx / `web.yml`            |
| **Prometheus → Alertmanager** | Prometheus authenticates _to_ Alertmanager | `basic_auth` in `alerting:`  |
