# 📌 **Typical Log Volume per Microservice**

Across real AKS/EKS/GKE workloads using container stdout + JSON logs:

| Service Type                           | Typical Log Rate | Daily Volume   |
| -------------------------------------- | ---------------- | -------------- |
| **Light service**                      | 0.1–0.5 MB/min   | 150–700 MB/day |
| **Medium service**                     | 1–5 MB/min       | 1.5–7 GB/day   |
| **Heavy service** (API, DB, messaging) | 5–20 MB/min      | 7–30 GB/day    |

A realistic average for a production microservice is:

👉 **~5 GB/day per service**

This is the number most SRE teams use for planning.

---

## 📌 **Your Case: 20 Microservices**

Using the realistic average:

$$
20 \cdot 5\text{ GB/day} = 100\text{ GB/day}
$$

### **Monthly (30 days)**

$$
100\text{ GB/day} \cdot 30 = 3\text{ TB/month}
$$

So a **reasonable baseline** for 20 microservices is:

👉 **~3 TB of logs per month**

---

## 📌 But here’s the real-world truth

Log volume varies massively depending on:

### **1. Log verbosity**

- DEBUG → explodes storage
- INFO → normal
- WARN/ERROR → minimal

### **2. JSON vs plain text**

JSON logs are **2–4× larger**.

### **3. Log fan-out**

If you log:

- request body
- response body
- stack traces
- retries
- DB queries

…your volume can jump to **20–50 GB/day per service**.

### **4. Sidecars**

Envoy/Istio access logs alone can be **10–30 GB/day**.

---

## 📌 **Realistic ranges for 20 microservices**

| Environment                                    | Daily Logs | Monthly Logs |
| ---------------------------------------------- | ---------- | ------------ |
| **Light** (INFO only, no sidecars)             | 20–40 GB   | 0.6–1.2 TB   |
| **Medium** (normal production)                 | 80–150 GB  | 2.4–4.5 TB   |
| **Heavy** (API gateways, retries, JSON logs)   | 200–500 GB | 6–15 TB      |
| **Very Heavy** (Istio, DEBUG, payload logging) | 1–2 TB     | 30–60 TB     |

Most companies with 20 microservices land in:

👉 **2–5 TB per month**

---

## 📌 **Logs vs Traces: Which is bigger?**

In most systems:

- **Logs are 5–20× larger than traces**
- Logs grow linearly with traffic
- Traces grow with sampling

If you store logs for 1 month and traces for 1 month, logs will dominate your storage bill.
