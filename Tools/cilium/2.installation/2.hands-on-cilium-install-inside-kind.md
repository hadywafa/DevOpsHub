# Hands-on Install Cilium in Kind Cluster

## 📌 Prerequisites

- [📚 Kind](https://kind.sigs.k8s.io/docs/user/quick-start/#installation)
- [📚 Cilium CLI](https://docs.cilium.io/en/v1.11/gettingstarted/k8s-install-helm/)

## 📃 Setup Kind Cluster

### kind config.yaml

```yaml
apiVersion: kind.x-k8s.io/v1alpha4
kind: Cluster
networks:
  ipFamily: dual
  disableDefaultCNI: true
nodes:
  - role: control-plane
  - role: worker
```

### Create Kind Cluster

```bash
kind create cluster --config kind-config.yaml
```

---

## 📃 Install Cilium CLI

- [📚 Cilium CLI](https://docs.cilium.io/en/stable/gettingstarted/k8s-install-default/#install-the-cilium-cli)

## 📃 Install Cilium

- [📚 Cilium](https://docs.cilium.io/en/stable/gettingstarted/k8s-install-default/#install-cilium)

```bash
cilium install
```

```bash
cilium status
```

```bash
cilium connectivity test
```

> if you want to see default helm values that cilium uses:
>
> ```bash
> cilium install --dry-run > cilium.yaml
> ```
