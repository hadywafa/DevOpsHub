# Limit Node Access

## Account Types

1. User Account
   - eg. `dev`, `test`, `prod`
2. Super Account
   - `UD=0`
   - `root`
3. System Account
   - eg. `ssh`, `kubelet`
4. Service Account
   - eg. `nginx`, `http`

## Useful Linux Commands

### 📌 `id`

```bash
id
# uid=1000(hady) gid=1000(hady) groups=1000(group1),1001(group2)
```

### 📌 `groups`