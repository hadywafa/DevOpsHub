# 🔍 **Identify and Disable Open Ports**

```bash
# list services listening on port 53
cat /etc/services | grep -w 53
```

```bash
#install net-tools
sudo apt-get install net-tools

# List open ports
netstat -tulpn | grep -w LISTEN  | grep 53
```
