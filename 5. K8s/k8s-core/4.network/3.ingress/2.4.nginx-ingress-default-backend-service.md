# 🛣️ **Default Backend in Ingress-NGINX**

## ✅ Where the Default Backend Is Actually Defined

In **Ingress-NGINX**, the default backend is **not configured in the Ingress resource itself**, but rather via a **flag in the Ingress controller Deployment**, like this:

```yaml
containers:
  - name: controller
    args:
      - /nginx-ingress-controller
      - --default-backend-service=app-space/default-backend-service
```

This tells the controller:

> “If no Ingress rule matches the request (host/path), route it to the `default-backend-service` in the `app-space` namespace.”

---

## 🔁 What Happens Without a Match

- If a request **does not match any Ingress rule** (host/path/class), the controller **falls back to the default backend**.
- If no `--default-backend-service` is set, the controller returns a **404 Not Found**.

---

## 🧪 Example: Default Backend in Action

### 🧱 1. Default Backend Service & Deployment

```yaml
apiVersion: v1
kind: Service
metadata:
  name: default-backend-service
  namespace: app-space
spec:
  selector:
    app: default-backend
  ports:
    - port: 80
      targetPort: 80
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: default-backend
  namespace: app-space
spec:
  replicas: 1
  selector:
    matchLabels:
      app: default-backend
  template:
    metadata:
      labels:
        app: default-backend
    spec:
      containers:
        - name: default-backend
          image: k8s.gcr.io/defaultbackend-amd64:1.5
          ports:
            - containerPort: 80
```

### ⚙️ 2. Ingress-NGINX Controller Deployment Snippet

```yaml
args:
  - /nginx-ingress-controller
  - --default-backend-service=app-space/default-backend-service
```

---

## 🧠 CKS Tip

This is a **controller-level configuration**, not part of the Ingress resource. It’s a common exam trap — so your correction is exactly the kind of detail that earns points.
