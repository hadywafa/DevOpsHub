# 📌 **Typical Trace Size in Production**

Across real systems using OpenTelemetry + JSON exporters:

| Component                                       | Typical Size        |
| ----------------------------------------------- | ------------------- |
| **One span**                                    | 1 KB – 5 KB         |
| **One trace** (5–20 spans)                      | **10 KB – 100 KB**  |
| **Heavy traces** (DB calls, retries, messaging) | **150 KB – 500 KB** |

For high‑traffic microservices, the **realistic average trace size** is:

👉 **~50 KB per trace** (safe planning number)

---

## 📌 Step-by-Step Estimation Model

Let’s define variables:

- \( S = 20 \) microservices
- \( R = \) requests per second (RPS) hitting the system
- \( T = \) sampling rate (1.0 = 100%)
- \( Z = \) average trace size (50 KB recommended)
- \( D = 86400 \) seconds per day

### **Daily trace volume**

$$
\text{Daily Volume} = R \cdot T \cdot D \cdot Z
$$

### **Monthly trace volume**

$$
\text{Monthly Volume} = 30 \cdot \text{Daily Volume}
$$

---

## 📌 **Let’s plug in realistic production numbers**

### Scenario A — **Moderate load**

- 200 RPS total across 20 services
- 100% sampling
- 50 KB per trace

$$
200 \cdot 86400 \cdot 50\text{ KB} = 864 \text{ GB/day}
$$

**Monthly: ~26 TB:**

---

### Scenario B — **High load**

- 1000 RPS
- 100% sampling
- 50 KB per trace

$$
1000 \cdot 86400 \cdot 50\text{ KB} = 4.32 \text{ TB/day}
$$

**Monthly: ~129 TB:**

---

### Scenario C — **Realistic production sampling (5%)**

- 1000 RPS
- 5% sampling
- 50 KB per trace

$$
1000 \cdot 0.05 \cdot 86400 \cdot 50\text{ KB} = 216 \text{ GB/day}
$$

**Monthly: ~6.5 TB:**

---

## 📌 **What most companies actually store**

Most production teams **never store 100% traces for 1 month**.

Typical patterns:

| Sampling          | Retention | Monthly Size |
| ----------------- | --------- | ------------ |
| **100% sampling** | 1–3 days  | 1–10 TB      |
| **10% sampling**  | 7–14 days | 2–5 TB       |
| **1–5% sampling** | 30 days   | 1–10 TB      |

If you want **1 month retention**, you _must_ use sampling.

---

## 📌 **Your likely real-world estimate**

Given your environment (20 microservices, heavy production load), the realistic range is:

### **➡️ 5–30 TB per month**

(depending on RPS and sampling)

If you give me your approximate **RPS**, I can compute a precise number.
